Flask>=2.0
Pillow>=8.0
numpy>=1.20
# Reed-Solomon implementations - one of these should satisfy the library import
reedsolo>=1.5    # common package providing RSCodec
reed_solomon>=0.2 # alternative package name if used by this project
